// import { IGame } from '../types/types'
// import { api } from './api'

// export const fetchGames = async (params?: Record<string, string | number>): Promise<IGame[]> => {
//     const { data } = await api.get('/games', { params })
//     return data
// }

// export const fetchGameById = async (id: number): Promise<IGame[]> => {
//     const { data } = await api.get('/game', {
//         params: {
//             id: id
//         }
//     })

//     return data
// }
